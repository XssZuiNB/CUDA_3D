#pragma once

#include<string>


namespace gca {
class device
{
private:
    std::string name;

public:
    device(/* args */);
    ~device();
};

device::device(/* args */)
{
}

device::~device()
{
}
}